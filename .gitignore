node_modules/
.pnpm-store/
dist/
build/
.next/
.next-*/
out/
coverage/
*.tsbuildinfo

# Python
__pycache__/
*.py[cod]
.venv/
venv/
.pytest_cache/
.ruff_cache/
.mypy_cache/
uv.lock

# Env & secrets
.env
.env.local
.env.*.local
!.env.example

# Local data & artifacts
/data/*
/tmp/

# The intake drop directories and their instructions are part of the repo; the
# documents dropped into them are somebody's local copy of a government file and
# are not ours to redistribute (docs/08 section 5).
!/data/intake/
/data/intake/*
!/data/intake/README.md
!/data/intake/*/
/data/intake/*/*
!/data/intake/*/.gitkeep
.artifacts/
*.log

# OS / editor
.DS_Store
Thumbs.db
.idea/
.vscode/*
!.vscode/extensions.json

# Internal design and requirement docs. Kept on disk for development (the code
# and CLAUDE.md reference them by path), but never published to the public
# repository. The compliance and disclaimer doc is the deliberate exception,
# because the site's disclaimer links to it.
/docs/*.md
!/docs/08-legal-compliance.md

# Local agent state (drift sentinel pause records)
/.agent-state/
